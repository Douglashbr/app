import styled from 'styled-components';

export const Button = styled.input`
    background-color: var(--color-first);
    color: #ffffff;
    height: 54px;
    border-radius: 27px;
    font-size: 1.428em;
    margin-top: var(--spacing-big);
    box-shadow: 0 2px 2px 0 var(--color-first-light);
    width: 100%;
    text-transform: uppercase;
    display: flex;
    justify-content: center;
    align-items: center;
    letter-spacing: 3px;
    cursor: pointer;
`;

